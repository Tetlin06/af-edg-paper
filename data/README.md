# Local structural data

This repository does not store PDB, AlphaFold DB, or SIFTS files.

Expected layout:

data/PDB/<PDB_ID>.pdb
data/AFDB/<AFDB_ID>.pdb
data/SIFTS/<pdb_id>.xml or <pdb_id>.xml.gz

The generated mapping cache is local and is not committed.
